import {View} from "react-native";
import {Button} from "react-native-paper";

export default function Account() {
    return (
        <View>
            <Button mode="elevated">Sign in</Button>
        </View>
    );
}
